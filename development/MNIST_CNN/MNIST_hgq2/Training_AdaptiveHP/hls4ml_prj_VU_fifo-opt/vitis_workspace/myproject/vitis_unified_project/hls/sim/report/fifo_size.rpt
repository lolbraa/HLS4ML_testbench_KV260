
This result is generated under the condition there is no block from testbench side.
So in real working environment, the suggested depth may need some margin.

After FIFO sizing, module total executing time has no change.(still 8034 cycles)
The detailed FIFO info are listed below.
    FIFO(AESL_inst_myproject_axi_master.gmem_out0_ptr_layer13_out_c_U):
    {
        The user setting depth:       4.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO never blocks its consumer and producer.
            The possible reason is the FIFO depth is big enough.
    }

    FIFO(AESL_inst_myproject_axi_master.batch_size_c1_U):
    {
        The user setting depth:       2.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO never blocks its consumer and producer.
            The possible reason is the FIFO depth is big enough.
    }

    FIFO(AESL_inst_myproject_axi_master.stream_in0_input_layer_U):
    {
        The user setting depth:       128.
        The smallest suggest depth:       128.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer and its producer in pairs.
            When producer starts writing, it is immediately blocked.
            When consumer starts reading, it is immdeiately blocked.
            The possible reason is the FIFO depth is not big enough to make the consumer and producer work smoothly.
    }

    FIFO(AESL_inst_myproject_axi_master.batch_size_c_U):
    {
        The user setting depth:       2.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.stream_out0_layer13_out_U):
    {
        The user setting depth:       128.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer3_out_U):
    {
        The user setting depth:       676.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer4_out_U):
    {
        The user setting depth:       676.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer5_out_U):
    {
        The user setting depth:       169.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer7_out_U):
    {
        The user setting depth:       121.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer8_out_U):
    {
        The user setting depth:       121.
        The smallest suggest depth:       3.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

    FIFO(AESL_inst_myproject_axi_master.compute_U0.grp_myproject_fu_378.layer9_out_U):
    {
        The user setting depth:       25.
        The smallest suggest depth:       2.
        Involved in deadlock path:        No.
        Work mode:
            FIFO always blocks its consumer.
            FIFO may sometimes or never block its producer.
            The possible reason is the producer works slowly for this channel.
    }

